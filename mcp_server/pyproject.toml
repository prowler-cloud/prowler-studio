[project]
dependencies = [
  "httpx>=0.28.1",
  "mcp[cli]>=1.5.0"
]
description = "Add your description here"
name = "prowler-studio-mcp-poc"
readme = "README.md"
requires-python = ">=3.13"
version = "0.1.0"

[project.scripts]
mcp-server-prowler-studio = "mcp_server_prowler_studio:main"
